@model IEnumerable<YetkiliyeBildir.Models.Report>
@{
    ViewData["Title"] = "İhbarlar";
}
<h2 class="fw-bold mb-4 text-theme" style="font-family:'Inter','Roboto',Arial,sans-serif;font-weight:700;">İhbarlar</h2>
<a class="btn btn-theme fw-bold rounded-pill mb-3 px-4" asp-action="Create" style="font-family:'Inter','Roboto',Arial,sans-serif;font-weight:600;">Yeni İhbar</a>
<table class="table table-bordered table-striped align-middle shadow-sm bg-white rounded-4" style="font-family:'Inter','Roboto',Arial,sans-serif;font-size:16px;">
    <thead>
        <tr>
            <th>Kategori</th>
            <th>Açıklama</th>
            <th>Durum</th>
            <th>Tarih</th>
            <th>Fotoğraf</th>
            <th>Konum</th>
            <th>Firma Aksiyonu</th>
            <th>Firma Notu</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Category</td>
                <td>@item.Description</td>
                <td>@item.Status</td>
                <td>@item.CreatedAt.ToString("g")</td>
                <td>
                    @if (!string.IsNullOrEmpty(item.PhotoPath))
                    {
                        <img src="@item.PhotoPath" alt="Fotoğraf" width="80" />
                    }
                </td>
                <td>
                    @if (item.Latitude != null && item.Longitude != null)
                    {
                        <a href="https://www.google.com/maps?q=@item.Latitude,@item.Longitude" target="_blank">Haritada Göster</a>
                    }
                </td>
                <td>@item.FirmaAksiyon</td>
                <td>@item.FirmaNotu</td>
            </tr>
        }
    </tbody>
</table> 